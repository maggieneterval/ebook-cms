const initialState = [
  {title: 'Chapter Zero'},
  {title: 'Chapter One'}
];

const chapters = (state=initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default chapters;
